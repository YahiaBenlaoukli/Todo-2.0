# TCP/IP model

# Network Access Layer

internet Layer
\#   Transport Layer
\# Application Layer

# network protocols

TCP(transmission control protocol)
ensures communication between computer
======================================

Arp (Adrress resolution protocol)
finds the mac address of the device using the **ip adrress**
\*vulnerable to ARP spoofing -> attacker pretends to be another device (MITM attack)
====================================================================================
